/*
 * The FidelityFX Vulkan frame-interpolation sources use the Windows SDK's
 * conventional `Windows.h` spelling.  MinGW's headers are lowercase on the
 * case-sensitive build hosts used by q2pro, so provide a portable shim.
 */
#ifndef Q2PRO_FSR3_WINDOWS_H
#define Q2PRO_FSR3_WINDOWS_H

#include <windows.h>

#endif
